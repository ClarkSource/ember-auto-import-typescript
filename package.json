{
  "private": true,
  "workspaces": [
    "src",
    "tests/*"
  ],
  "scripts": {
    "lint:js": "yarn workspaces run lint:js",
    "test": "yarn workspace @test/addon test && yarn workspace @test/app test"
  },
  "volta": {
    "node": "16.16.0"
  }
}
